# Local: /api (Vite proxy). Vercel production: /api (same-origin serverless).
# Remote backend only: https://your-api.example.com/api
VITE_API_URL=/api

# "false" = real backend. "true" = in-browser mock (local UI only).
VITE_USE_MOCK_API=false
