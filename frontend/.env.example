# Local dev: /api (Vite proxy → localhost:3001)
# Vercel Services: /_/backend/api (see vercel.json)
VITE_API_URL=/api

# "false" = real backend. "true" = in-browser mock (local UI only).
VITE_USE_MOCK_API=false
