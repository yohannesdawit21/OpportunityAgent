PORT=3001
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: override the default model (gemini-2.0-flash). Falls back automatically if unavailable.
GEMINI_MODEL=gemini-2.0-flash
# Set to "true" to skip the Gemini API and use deterministic seed responses
USE_AGENT_FALLBACK=false
